/**
 * Composition API
 */

// Workaround for https://github.com/vuejs/composition-api/issues/535
import Vue from 'vue';
import VueCompositionAPI from '@vue/composition-api';
Vue.use(VueCompositionAPI);
